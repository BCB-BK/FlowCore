# PROD-Schutz für FlowCore — vorbereitet aus dem Server-Register am 10.09.2026.
#
# HERKUNFT: server-register.md, Umgebungstabelle Tool-Server (05.08.2026 gemessen)
# Der Agent auf der Maschine prüft diese Werte beim ersten Lauf gegen die
# Wirklichkeit (Kernvertrag §5: Maschinenfakten kommen von der Maschine) und
# korrigiert sie hier. Bis dahin gilt: im Zweifel geschützt.

PFAD   /var/www/flowcore-prod
DB     flowcore_prod
HOST   flowcore.onecampusgroup.de

# DEV (frei): /var/www/flowcore-dev, flowcore_dev, dev-flowcore.onecampusgroup.de
# Übernahme nach PROD läuft über `sudo flowcore-deploy prod` — nie per Pull.
