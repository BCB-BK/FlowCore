# Post-Deploy-Smoke — PROD (Branch master, Port 5002)
# Erwartete Status real nachgemessen am 30.08.2026.
BASE_URL=https://flowcore.onecampusgroup.de
HEALTH=/api/healthz
# Die Oberfläche wird ausgeliefert (SPA-index.html).
CHECK=/|200
# Geschützte API: 401 ist hier das RICHTIGE Ergebnis. Ein 200 hiesse, die
# Anmeldepflicht ist ausgefallen; ein 502, der Dienst antwortet gar nicht.
CHECK=/api/nodes|401
TIMEOUT=15
